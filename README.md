# Steel ERP Information Flow Simulator

Offline-ready executive simulator for an integrated steel manufacturing ERP. Open `index.html` directly in a browser. The application uses semantic modules, SVG flow visualization, scenario controls, KPI cards and a department detail panel. CDN links for GSAP/Chart.js are optional enhancements; core navigation and simulation run without a build step.

